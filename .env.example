
# PLAYWRIGHT_MCP_URL = MCP endpoint, normally ending in /mcp
PLAYWRIGHT_MCP_URL=https://your-playwright-mcp-service.onrender.com/mcp
# PLAYWRIGHT_MCP_TOKEN = Bearer token if authentication is configured
PLAYWRIGHT_MCP_TOKEN=
